{
  "scripts": {
    "build-css": "npx tailwindcss -i input.css -o ./public/css/style.css --watch",
    "dev": "concurrently \"air\" \"templ generate --watch\" \"npx tailwindcss -i input.css -o ./public/css/style.css --watch\""
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
